server:
 port: 9092

spring:
  application:
    name: productapp
   
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/maybank
    username: root
    password: root

  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    hibernate.format_sql: true